apiVersion: policy.elotl.co/v1alpha1
kind: SchedulePolicy
metadata:
  name: kubeflow-ns-policy
spec:
  spreadConstraints:
    spreadMode: Duplicate
    topologyKey: kubernetes.io/metadata.name
  resourceSelectors:
    labelSelectors:
    - matchLabels:
        kubernetes.io/metadata.name: kubeflow
  namespaceSelector:
    matchExpressions:
    - key: kubernetes.io/metadata.name
      operator: Exists
  groupBy:
    labelKey: kubernetes.io/metadata.name
